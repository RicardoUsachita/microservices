spring:
  application:
    name: planillaGenerate
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/planilla
    username: root
    password: 11235813
  jpa:
    hibernate:
      ddl-auto: create-drop
