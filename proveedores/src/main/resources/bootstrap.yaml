spring:
  application:
    name: proveedores
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/proveedores
    username: root
    password: 11235813
  jpa:
    hibernate:
      ddl-auto: create-drop
